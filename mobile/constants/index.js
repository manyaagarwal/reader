import { DefaultTheme } from "react-native-paper";

export const appTheme = {
  ...DefaultTheme,
  colors: {
    ...DefaultTheme.colors,
    primary: "cyan",
    accent: "black",
  },
};

export const lightTheme = {};
